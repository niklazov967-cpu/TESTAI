# Как включить Multi-Model режим в Cursor

## ⚠️ Важно: .cursorrules НЕ включает multi-model автоматически

Файл `.cursorrules` содержит **инструкции для моделей**, но **не активирует** использование нескольких моделей одновременно. Это нужно настроить отдельно.

## 🔧 Способы включения Multi-Model режима

### Способ 1: Через настройки Cursor (GUI)

1. **Откройте настройки Cursor:**
   - `Cmd + ,` (Mac) или `Ctrl + ,` (Windows/Linux)
   - Или: `Cursor` → `Settings` → `Features` → `AI`

2. **Найдите раздел "AI Models" или "Multi-Model":**
   - Включите опцию "Use Multiple Models" или "Multi-Model Mode"
   - Выберите модели: **Composer 1** и **Sonnet 4.5**

3. **Настройте Cycle Agent Count:**
   - Установите значение (например, 2-3)

### Способ 2: Через Command Palette

1. Откройте Command Palette: `Cmd/Ctrl + Shift + P`
2. Введите: `Cursor: Enable Multi-Model`
3. Выберите модели: Composer 1 и Sonnet 4.5

### Способ 3: Через явное указание в промптах

В ваших запросах к Cursor явно указывайте модели:

```
@Composer1 создай новый endpoint
@Sonnet4.5 проверь этот код
```

Или в одном запросе:
```
Используя Composer 1 для реализации и Sonnet 4.5 для проверки, создай...
```

## 📝 Обновление .cursorrules для явного указания моделей

Файл `.cursorrules` уже обновлен с явными инструкциями использовать обе модели. Но этого недостаточно!

## ⚠️ Главная проблема

**Файл `.cursorrules` НЕ активирует multi-model режим автоматически!**

Cursor может игнорировать инструкции из `.cursorrules` и использовать только одну модель по умолчанию, если:
1. Multi-model режим не включен в настройках Cursor
2. Вы не указываете модели явно в промптах
3. Версия Cursor не поддерживает multi-model

## ✅ Решение: Явное указание моделей в промптах

### Способ 1: Использовать @ упоминания

В каждом запросе явно указывайте модели:

```
@Sonnet4.5 Проанализируй архитектуру поиска сталей и создай план улучшений
@Composer1 Реализуй улучшения согласно плану от Sonnet 4.5
@Sonnet4.5 Проверь реализацию и найди проблемы
```

### Способ 2: Использовать Composer режим

1. Откройте Composer в Cursor (`Cmd/Ctrl + I`)
2. В начале промпта укажите:
```
Используя multi-model режим:
1. Sonnet 4.5 должен проанализировать задачу
2. Composer 1 должен реализовать решение
3. Sonnet 4.5 должен проверить результат

Задача: [ваша задача]
```

### Способ 3: Проверить настройки Cursor

1. Откройте настройки: `Cmd/Ctrl + ,`
2. Найдите: `Cursor Settings` → `Features` → `AI`
3. Проверьте:
   - Включен ли "Multi-Model Mode"?
   - Выбраны ли обе модели (Composer 1 и Sonnet 4.5)?
   - Не установлена ли одна модель по умолчанию?

## 🔍 Диагностика проблемы

Если Cursor все равно использует только одну модель:

1. **Проверьте версию Cursor:**
   - Multi-model может быть доступен только в определенных версиях
   - Обновите Cursor до последней версии

2. **Проверьте подписку:**
   - Multi-model режим может требовать определенный уровень подписки

3. **Проверьте логи Cursor:**
   - Посмотрите, какие модели реально используются
   - Может быть видно в консоли разработчика Cursor

## 💡 Альтернативное решение

Если multi-model не работает, можно использовать последовательные запросы:

```
Запрос 1: "Sonnet 4.5, проанализируй [задача] и создай план"
Запрос 2: "Composer 1, реализуй [задача] согласно плану от предыдущего ответа"
Запрос 3: "Sonnet 4.5, проверь реализацию от Composer 1"
```

Это не идеально, но работает как замена multi-model режиму.
