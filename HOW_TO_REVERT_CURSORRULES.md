# Как вернуться к состоянию без .cursorrules

## 📋 Информация

Файл `.cursorrules` был создан в коммите `88de63e`.
Коммит до создания файла: `5531f8d` (там файла `.cursorrules` не было).

## 🔄 Способы отката

### Способ 1: Просто удалить файл (рекомендуется)

```bash
# Удалить файл .cursorrules
rm .cursorrules
```

**Что произойдет:**
- Cursor вернется к настройкам по умолчанию
- Не будет никаких специальных правил для моделей
- Cycle Agent Count будет использовать значения по умолчанию Cursor (обычно 3-5 циклов)

### Способ 2: Через git (восстановить состояние до создания файла)

```bash
# Вариант A: Удалить файл из текущего состояния
git rm .cursorrules
git commit -m "Remove .cursorrules - return to default Cursor settings"

# Вариант B: Восстановить состояние проекта до создания файла
git checkout 5531f8d -- .
# ВНИМАНИЕ: Это восстановит ВСЕ файлы к состоянию того коммита!
```

### Способ 3: Временно отключить (переименовать)

```bash
# Переименовать файл, чтобы Cursor его не видел
mv .cursorrules .cursorrules.backup

# Чтобы вернуть обратно:
mv .cursorrules.backup .cursorrules
```

## ⚠️ Что произойдет после удаления

После удаления `.cursorrules`:

1. **Cursor вернется к настройкам по умолчанию**
   - Cycle Agent Count будет определяться автоматически Cursor
   - Обычно это 3-5 циклов для сложных задач

2. **Не будет распределения ролей между моделями**
   - Cursor будет использовать одну модель по умолчанию
   - Или модели будут работать без специальных инструкций

3. **Не будет специфичных правил для вашего проекта**
   - Не будет инструкций по работе с промптами
   - Не будет специфики для поиска аналогов сталей

## 📊 Сравнение: с .cursorrules vs без него

| Параметр | С .cursorrules | Без .cursorrules |
|----------|----------------|------------------|
| Cycle Agent Count | 1-5 циклов (настраиваемо) | 3-5 циклов (по умолчанию) |
| Распределение ролей | Composer 1 + Sonnet 4.5 | Одна модель по умолчанию |
| Специфика проекта | Есть инструкции | Нет |
| Контроль циклов | Явный контроль | Автоматический |

## 🎯 Рекомендации

### Если тратится слишком много токенов:

1. **Сначала попробуйте уменьшить Cycle Agent Count в .cursorrules:**
   ```markdown
   ### Cycle Agent Count
   - Простые задачи: 1 цикл
   - Средние задачи: 1-2 цикла
   - Сложные задачи: 2 цикла
   ```

2. **Если это не помогает - удалите файл:**
   ```bash
   rm .cursorrules
   ```

3. **Мониторьте использование токенов:**
   - Проверяйте баланс API в настройках
   - Отслеживайте количество циклов в логах Cursor

## 🔧 Быстрый откат (скрипт)

Создайте файл `revert_cursorrules.sh`:

```bash
#!/bin/bash
# Скрипт для отката .cursorrules

echo "Удаление .cursorrules..."
rm .cursorrules

echo "✅ Файл удален. Cursor вернется к настройкам по умолчанию."
echo "📝 Чтобы вернуть файл обратно, используйте: git checkout HEAD -- .cursorrules"
```

Использование:
```bash
chmod +x revert_cursorrules.sh
./revert_cursorrules.sh
```

## 📝 Восстановление файла обратно

Если вы удалили файл и хотите вернуть его:

```bash
# Восстановить из последнего коммита
git checkout HEAD -- .cursorrules

# Или восстановить конкретную версию
git checkout 88de63e -- .cursorrules
```

## 💡 Альтернатива: Минимальная версия .cursorrules

Если хотите сохранить файл, но минимизировать циклы:

```markdown
# Минимальная конфигурация Cursor

### Cycle Agent Count (минимизировано)
- Простые задачи: 1 цикл
- Средние задачи: 1 цикл
- Сложные задачи: 2 цикла (максимум)
```

Это даст вам контроль над циклами, но с минимальным расходом токенов.
