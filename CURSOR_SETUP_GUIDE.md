# Пошаговая инструкция по настройке Cursor IDE

## 🔍 Где найти настройки моделей AI в Cursor

### Способ 1: Через Command Palette (САМЫЙ НАДЁЖНЫЙ)

1. **Нажмите:** `Cmd + Shift + P` (Mac) или `Ctrl + Shift + P` (Windows/Linux)

2. **Введите команду:**
   ```
   Preferences: Open User Settings (JSON)
   ```

3. **Добавьте в файл settings.json следующие настройки:**

```json
{
  "cursor.aiModel": "claude-3.5-sonnet",
  "cursor.chatModel": "claude-3.5-sonnet",
  "cursor.composerModel": "claude-3.5-sonnet",
  "cursor.autocompleteModel": "claude-3.5-sonnet",
  "cursor.enableAutoComplete": true,
  "cursor.enableInlineCompletion": true,
  "cursor.maxTokens": 8000,
  "cursor.temperature": 0.2,
  "cursor.maxContextFiles": 15
}
```

4. **Сохраните файл:** `Cmd + S` (Mac) или `Ctrl + S` (Windows/Linux)

### Способ 2: Через меню Settings

1. **Откройте настройки:**
   - Mac: `Cmd + ,` (запятая)
   - Windows/Linux: `Ctrl + ,` (запятая)

2. **В поиске настроек (вверху) введите:**
   - `cursor model`
   - или `ai model`
   - или `claude`

3. **Найдите и измените:**
   - `Cursor: AI Model` → выберите `claude-3.5-sonnet`
   - `Cursor: Chat Model` → выберите `claude-3.5-sonnet`
   - `Cursor: Composer Model` → выберите `claude-3.5-sonnet`
   - `Cursor: Autocomplete Model` → выберите `claude-3.5-sonnet`

### Способ 3: Через интерфейс чата

1. **Откройте чат Cursor:**
   - Нажмите `Cmd/Ctrl + K`

2. **В правом верхнем углу чата найдите:**
   - Иконку шестерёнки ⚙️
   - Или три точки `...`
   - Или название текущей модели (кликните на неё)

3. **Выберите модель:** `claude-3.5-sonnet`

## 🎯 Быстрая настройка (РЕКОМЕНДУЕТСЯ)

**Самый простой способ:**

1. Нажмите `Cmd + Shift + P` (или `Ctrl + Shift + P`)
2. Введите: `Preferences: Open User Settings (JSON)`
3. Скопируйте и вставьте настройки из "Способ 1" выше
4. Сохраните (`Cmd + S`)

## ✅ Проверка настройки

1. Откройте чат: `Cmd/Ctrl + K`
2. Введите: "Привет"
3. В интерфейсе чата должна отображаться модель `claude-3.5-sonnet`

## 🔧 Если настройки не применяются

1. **Перезапустите Cursor** полностью (закройте и откройте заново)
2. **Проверьте версию Cursor:** `Cursor` → `About Cursor`
3. **Обновите Cursor**, если версия устарела

## 📍 Где находится settings.json

**macOS:**
```
~/Library/Application Support/Cursor/User/settings.json
```

**Windows:**
```
%APPDATA%\Cursor\User\settings.json
```

**Linux:**
```
~/.config/Cursor/User/settings.json
```

## 💡 Полезные команды

- `Cmd/Ctrl + Shift + P` - Command Palette (открыть команды)
- `Cmd/Ctrl + K` - Открыть чат Cursor
- `Cmd/Ctrl + L` - Открыть Composer
- `Cmd/Ctrl + ,` - Открыть настройки

## 🆘 Если ничего не помогает

1. Убедитесь, что вы используете **Cursor IDE**, а не VS Code
2. Проверьте, что у вас активна подписка Pro+ ($60)
3. Попробуйте переустановить Cursor с официального сайта: https://cursor.com
