# Актуальная инструкция по настройке Cursor IDE 2.1.20

## 🎯 Для тарифа Pro+ ($60/месяц) доступны модели:

- **Claude 4 Sonnet** ⭐ (рекомендуется)
- **Gemini 2.5 Pro**
- **GPT-4.1**
- **Auto** (автоматический выбор модели)

⚠️ **Важно:** Claude 3.5 Sonnet больше не доступна, минимальная версия - Claude 4.

## 🔍 Способ 1: Выбор модели через интерфейс чата (САМЫЙ ПРОСТОЙ)

1. **Откройте чат Cursor:**
   - Нажмите `Cmd + K` (Mac) или `Ctrl + K` (Windows/Linux)
   - Или нажмите на иконку Cursor в боковой панели

2. **Найдите выпадающий список с моделью:**
   - В верхней части окна чата
   - Или рядом с полем ввода
   - Обычно там написано текущая модель (например, "Claude 4 Sonnet" или "Auto")

3. **Кликните на название модели** и выберите:
   - **Claude 4 Sonnet** (рекомендуется для большинства задач)

## 🔍 Способ 2: Через настройки - раздел "Models"

1. **Откройте настройки:**
   - Mac: `Cmd + ,` (запятая)
   - Windows/Linux: `Ctrl + ,` (запятая)
   - Или: `Cursor` → `Settings`

2. **В поиске настроек введите:**
   - `models`
   - или `model`
   - или `ai`

3. **Найдите раздел "Models" или "AI Models"**

4. **Выберите модель:**
   - Найдите "Default Model" или "Chat Model"
   - Выберите **Claude 4 Sonnet**

## 🔍 Способ 3: Через Command Palette

1. **Откройте Command Palette:**
   - Mac: `Cmd + Shift + P`
   - Windows/Linux: `Ctrl + Shift + P`

2. **Введите:**
   ```
   Cursor: Change Model
   ```
   или
   ```
   Cursor: Select Model
   ```

3. **Выберите модель из списка:**
   - **Claude 4 Sonnet**

## 🔍 Способ 4: Через settings.json (если UI не работает)

1. **Откройте Command Palette:** `Cmd/Ctrl + Shift + P`

2. **Введите:**
   ```
   Preferences: Open User Settings (JSON)
   ```

3. **Добавьте или измените настройки:**

```json
{
  "cursor.model": "claude-4-sonnet",
  "cursor.chatModel": "claude-4-sonnet",
  "cursor.composerModel": "claude-4-sonnet",
  "cursor.autocompleteModel": "claude-4-sonnet",
  
  "cursor.enableAutoComplete": true,
  "cursor.enableInlineCompletion": true,
  "cursor.maxTokens": 8000,
  "cursor.temperature": 0.2,
  "cursor.maxContextFiles": 15
}
```

4. **Сохраните:** `Cmd + S` (Mac) или `Ctrl + S` (Windows/Linux)

5. **Перезапустите Cursor**

## 🎯 Рекомендации по выбору модели

### Claude 4 Sonnet ⭐ (РЕКОМЕНДУЕТСЯ)
- **Лучший баланс** качества и скорости
- Отлично работает с кодом
- Подходит для всех задач: чат, автодополнение, редактирование

### Gemini 2.5 Pro
- Более инициативная модель
- Может предлагать собственные решения
- Хороша для творческих задач

### GPT-4.1
- Отлично работает с TypeScript/JavaScript
- Быстрая обработка
- Хороша для рефакторинга

### Auto
- Автоматически выбирает лучшую модель для задачи
- Удобно, если не хотите думать о выборе
- Может использовать разные модели для разных задач

## ✅ Проверка настройки

1. Откройте чат: `Cmd/Ctrl + K`
2. Введите любой вопрос
3. В интерфейсе чата должна отображаться модель **Claude 4 Sonnet**

## 🔧 Если модель не меняется

1. **Перезапустите Cursor** полностью
2. **Проверьте версию:** `Cursor` → `About Cursor`
3. **Обновите Cursor**, если версия устарела
4. **Проверьте подписку:** `Cursor` → `Account` → убедитесь, что Pro+ активна

## 📍 Где находится settings.json

**macOS:**
```
~/Library/Application Support/Cursor/User/settings.json
```

**Windows:**
```
%APPDATA%\Cursor\User\settings.json
```

**Linux:**
```
~/.config/Cursor/User/settings.json
```

## 💡 Полезные команды

- `Cmd/Ctrl + K` - Открыть чат
- `Cmd/Ctrl + L` - Открыть Composer/Agent
- `Cmd/Ctrl + Shift + P` - Command Palette
- `Cmd/Ctrl + ,` - Настройки

## 🆘 Если ничего не помогает

1. Убедитесь, что используете **Cursor IDE**, а не VS Code
2. Проверьте, что у вас активна подписка **Pro+ ($60)**
3. Попробуйте обновить Cursor: `Cursor` → `Check for Updates`
4. Обратитесь в поддержку: https://cursor.com/support

